# Tornado web server

use python 2.6 - 2.7

Tide graph application.
fetching tidal data from Japan Meteorological Agency and Japan Coast Guard.
tide database used mongodb.

Usage:
    tornade <commands> <port>

Commands:
    start    starting tornado web server
    stop     stop tornado web server
    restart  stop && start 

Test command
    $ nosetests
